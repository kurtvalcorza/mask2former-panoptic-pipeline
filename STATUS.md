# Release status

Current status: **Candidate — tutorial source complete, local pre-flight executed, awaiting supported-runtime execution**. The pipeline package (`samples.py`, `pipeline.py`), offline unit tests (including the role-helper, import-boundary and both notebook-parity suites), a local CPU smoke run covering inference and the full adaptation path, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the two standalone tutorials — `tutorials/mask2former_panoptic_colab.ipynb` (`TASK-INFERENCE`) and `tutorials/mask2former_panoptic_finetune_colab.ipynb` (`E2E`), both generated by `tools/build_notebook.py` (DIMER Notebook Specification 2.0 §4) — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR3); one fresh-kernel execution of each notebook on the workstation is recorded in `docs/release-verification.md`. None of that is supported-runtime notebook evidence; the repository is not release-grade until a Colab/Kaggle execution of each notebook for the exact candidate commit is recorded there.
