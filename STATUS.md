# Release status

Current status: **Candidate — tutorial source complete, local pre-flight and clean-room Kaggle executions recorded, awaiting review for promotion**. The pipeline package (`samples.py`, `pipeline.py`), offline unit tests (including the role-helper, import-boundary and both notebook-parity suites), a local CPU smoke run covering inference and the full adaptation path, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the two standalone tutorials — `tutorials/mask2former_panoptic_colab.ipynb` (`TASK-INFERENCE`) and `tutorials/mask2former_panoptic_finetune_colab.ipynb` (`E2E`), both generated by `tools/build_notebook.py` (DIMER Notebook Specification 2.0 §4) — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR3); one fresh-kernel execution of each notebook on the workstation and one clean-room Kaggle execution of each committed blob (CPU for `TASK-INFERENCE`, Tesla T4 for `E2E`, both PASSED 2026-09-18) are recorded in `docs/release-verification.md`. Promotion to release-grade is a reviewer/integrator decision against those records for the exact notebook revision.
